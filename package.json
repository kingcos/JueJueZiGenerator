{
  "dependencies": {
    "clipboard": "^2.0.8",
    "lodash.clonedeep": "^4.5.0"
  },
  "devDependencies": {
    "@types/lodash.clonedeep": "^4.5.6",
    "parcel-bundler": "^1.12.5",
    "ts-node": "^10.2.1",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "dev": "parcel index.html -d dev",
    "build": "parcel build index.html -d build --public-url . --detailed-report",
    "prebuild": "rm -rf build"
  }
}